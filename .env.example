# GitHub credentials (required for the Sync feature)
# Personal Access Token with `repo` and `read:user` scopes
GITHUB_TOKEN=your_github_token_here

# Your GitHub username (used to query authored PRs)
GITHUB_USERNAME=your_username_here

# Optional: override the default SQLite database path
# DATABASE_URL=sqlite:////absolute/path/to/devdash.db
